<p id="notice"><%= notice %></p>

<h1><%= @person.id %></h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @names.each do |name| %>
      <tr>
        <td><%= name.core_name_type.display %></td>
        <td><%= name.first %></td>
        <td><%= name.middle %></td>
        <td><%= name.last %></td>
        <td><%# link_to 'Show', person %></td>
        <td><%= link_to 'Edit', controller: 'person_names', action: 'edit', person_id: @person %></td>
        <td><%# link_to 'Destroy', person, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Name Type', new_person_person_name_path %>
